name: Build
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-2019
    steps:
    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v1.0.2
    - name: Restore packages
      run: msbuild /t:restore '.\WinVolumeLimiter.sln'
    - name: Build
      run: msbuild '.\WinVolumeLimiter.sln'
